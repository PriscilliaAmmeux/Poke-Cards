import styled from "styled-components";

export default styled.article`
  .pledgeStore {
    display: flex;
    flex-direction: row;
    justify-content: space-around;
    align-items: center;
    margin-top: 0.5rem;
  }

  .buyButton {
    border-radius: 50px;
    border: none;
    padding: 0.5rem;
    cursor: pointer;
  }
`;
